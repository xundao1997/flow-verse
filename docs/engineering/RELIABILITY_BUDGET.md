# FlowVerse Reliability and Availability Budget

## Policy and Ownership

- High availability is a measured property, not an architecture label.
- Reliability targets and failure behavior require user or approved-package confirmation.
- Use the simplest topology that meets Confirmed targets.
- Do not infer replicas, regions, services, queues, caches, failover, health paths, or release strategy.
- This file owns availability, error, deadline/retry, idempotency, concurrency/backpressure, recovery, and failure-control targets.
- PERFORMANCE_BUDGET.md owns latency, throughput, bundle, interaction, and memory measurements; reference reliability row IDs instead of duplicating operational limits.

## State Model

Applicability values: Required, Optional, Unknown, N/A.

Target status values: Proposed, Confirmed, Conflict, Unknown, N/A.

Verification status values: NotYetTested, Passed, Failed, Unverified, N/A.

- At a due Gate stage, Applicability Unknown or a missing affected row blocks classification.
- A Required row must have target status Confirmed and verification status Passed before its due gate completes.
- Before the due gate, NotYetTested is valid and does not block unrelated earlier work.
- Optional does not block when omitted; if implemented or claimed, its target and verification must be Confirmed/Passed.
- N/A requires scope-specific evidence.

## Availability Target Registry

| Scope ID | Critical flow / dependency | Applicability | SLI and measurement | SLO / window | Error budget and exhaustion policy | Gate stage | Target evidence | Target status | Owner |
|---|---|---|---|---|---|---|---|---|---|
| FV1-PRODUCT-DESIGN | MVP product availability during real validation | Required | Successful usable service time over the approved validation period; exact monitoring sampling remains to be approved | 99% internal MVP target; not a commercial SLA | Exhaustion response/rollout policy TBD | Pre-release | PRD v1.1 section 7.9 | Confirmed | Delivery/operations owner TBD |
| FV1-PRODUCT-DESIGN | Work-home deterministic entry during Bot dependency failure | Required | Continue work, pending, and task list remain usable within the ordinary-page target during Bot wait/failure/policy block | Required for every exercised failure scenario | Bot failure may degrade only the Bot region; no fake success | Implementation | PRD v1.1 sections 7.5 and 7.9 | Confirmed | Product/application owner TBD |
| FV1-ROADMAP-REVIEW | Production core-service HA | Unknown | Separately measure usable application service, authoritative-writer availability, and AI/provider availability for the approved process/node/fault-domain/quorum/region envelope | 99.9% per month is only a proposal; the 99% internal-MVP target above remains the sole Confirmed availability number | Rollout stops or degrades only affected capabilities when the approved error budget is exhausted; exact policy TBD | Production architecture / pre-release | `V1_TECHNICAL_SOLUTION_PROPOSAL.md` sections 12.5–12.8; Proposed ADR-0022 | Proposed | Delivery/operations owner TBD |

## Recovery Target Registry

RTO/RPO rows are keyed by recoverable data set or data class, not by a stateless request flow.

| Scope ID | Data set / class | Applicability | RTO | RPO | Backup/restore scope | Gate stage | Target evidence | Target status | Verification status | Owner |
|---|---|---|---|---|---|---|---|---|---|---|
| FV1-PRODUCT-DESIGN | Confirmed formal content, releases, feedback, analyses, decisions, configuration associations, and audit evidence | Required | 4 hours | No confirmed formal record may be lost; exact backup frequency must satisfy this and remains to be designed | Persistent formal business data and required audit/configuration references | Pre-release | PRD v1.1 sections 7.9 and 7.11 | Confirmed product target | NotYetTested | Data/operations owner TBD |
| FV1-PRODUCT-DESIGN | Unconfirmed drafts and partial execution results | Required | 4 hours | At most 24 hours of unconfirmed drafts may be lost | Saved drafts and persisted partial results; client-only unsaved keystrokes excluded | Pre-release | PRD v1.1 section 7.9 | Confirmed | NotYetTested | Data/operations owner TBD |
| FV1-ROADMAP-REVIEW | Cross-store formal recovery set and consistent cut | Unknown | 4 hours inherited only if this row is approved for the same fault envelope | No confirmed formal record may be lost within the approved envelope; the exact mechanism remains Proposed | PostgreSQL PITR/backup plus durable object versions; choose and approve either checkpoint epoch+bounded PG MVCC manifest or monotonic object-reference watermark+immutable sharded manifest/Merkle root. Both bind a strict immutable component manifest containing PG timeline/LSN/cut, every object shard/Merkle root and backup coverage, independent deletion-ledger high-watermark, Schema/config ref/hash/version, compatible application artifact ref/hash and recovery runbook ref/hash/version. Only signed/checkpoint-hashed `BUILDING→VERIFYING→RECOVERABLE` records may open restore; any missing/mismatched component, current ledger replay or uniqueness reconciliation keeps it closed | Production architecture / pre-release | `V1_TECHNICAL_SOLUTION_PROPOSAL.md` sections 11.2–11.4; Proposed ADR-0018 | Proposed | Unverified | Data/operations/security owners TBD |

## Failure-Control Registry

Create separate rows when controls have different applicability or gates.

| Scope ID | Boundary / operation | Control type | Applicability | Gate stage | Confirmed requirement / configuration | Target evidence | Target status | Verification status | Owner |
|---|---|---|---|---|---|---|---|---|---|
| FV1-PRODUCT-DESIGN | Actual external release confirmation and Cycle creation | Idempotency / atomicity | Required | Implementation | Create both with exact immutable bindings or create neither; one task has at most one active Cycle | PRD v1.1 sections 7.5 and 7.11 | Confirmed | NotYetTested | Release/Cycle owner TBD |
| FV1-PRODUCT-DESIGN | Formal confirmation commands | Idempotency / concurrency | Required | Implementation | Reject duplicate/stale confirmation, preserve user input, and return authoritative outcome; exact key scope/retention TBD in architecture | PRD v1.1 product/UIUX formal-action contract | Confirmed product semantics | NotYetTested | Owning domain TBD |
| FV1-PRODUCT-DESIGN | Paid Bot/business AI execution | Capacity / backpressure | Required | Implementation | One user-level paid slot, one business step per task, up to three models per step; queued work is visible and cancellable before start | PRD v1.1 sections 3.10 and 7.9 | Confirmed | NotYetTested | AI execution owner TBD |
| FV1-PRODUCT-DESIGN | AI execution lifecycle | Deadline / cancellation / retry | Required | Implementation | Maximum 30 minutes; preserve completed outputs and cost on timeout; retry/model switch creates a new attempt; never silently switch or multiply retry | PRD v1.1 sections 3.10 and 7.9 | Confirmed | NotYetTested | AI execution owner TBD |
| FV1-PRODUCT-DESIGN | Model/provider call | Policy / isolation | Required | Implementation | Evaluate current green/yellow/red policy and allowed data scope before each execution; screenshots never enter model input | PRD v1.1 sections 3.16 and 7.11 | Confirmed | NotYetTested | AI policy owner TBD |
| FV1-ROADMAP-REVIEW | System decision Prompt, JIT provider call-start and formal command boundary | Isolation / Idempotency / Degradation | Unknown | Before first AI-enabled release | Model output can only be `candidate/abstain/needs_human_review`; trusted binding metadata and final PASS/BLOCK are server-owned. Before every provider call, API atomically revalidates job/purpose/evaluation arm+role/lane/attempt/step/binding and lease/fencing. BUSINESS additionally requires matching model-profile activation and latest eligible assessment. EVALUATION instead requires typed immutable authorization、comparison mode/basis/arm/order、EvaluationBinding/dataset/license/independent budget and no business-write capability：OFFLINE由管理员授权，SHADOW同时要求immutable rollout authority manifest与用户D01 consent。TARGET匹配CANDIDATE/CONTROL配置；typed HUMAN/NO_AI baseline以不可变artifact+人工批准receipt作为证据且不创建provider lane。JUDGE binding只冻结dependency selector，JIT按basis冻结resolved input：DIRECT=candidate receipt，PROMPT_ONLY=两个provider arm，BASELINE_GATE=candidate+baseline authority，FACTORIAL=冻结plan声明的组合。Both branches revalidate policy/budget/input/object/cancel/deletion and persist actual call input plus selected authority ref/hash/kind/arm/role, stable call intent/receipt and exact-key strategy. 单个评测结果只写artifact/progress，只有API finalizer完成全plan/hard-fail/对应basis/换位/必要人审才追加assessment；DIRECT不能单独晋升，evaluation execution不能创建business candidate/formal | `../ai/SYSTEM_DECISION_PROMPTS.md`; Proposed ADR-0029/0030 | Proposed | Unverified | AI governance / execution owner TBD |
| FV1-ROADMAP-REVIEW | PostgreSQL promotion, synchronous durability and writer endpoint | Isolation / Health / Recovery | Unknown | Production architecture / pre-release | Decision owner must select: (A) three data-bearing nodes with commits acknowledged by at least one eligible synchronous standby plus proven quorum/fencing control, or (B) writer+sole synchronous data standby+independent third vote/equivalent control, with all formal writes fail closed whenever that standby is lost/not synchronous. Both forbid promotion unless majority, eligibility and old-writer fencing are proven; no asynchronous risk window may be hidden as availability | `V1_TECHNICAL_SOLUTION_PROPOSAL.md`; Proposed ADR-0022 | Proposed | Unverified | Data/operations owner TBD |
| FV1-ROADMAP-REVIEW | Public API, internal Worker endpoint and capability readiness | Isolation / Health / Capacity | Unknown | Production architecture / pre-release | Public ingress has no route to `/internal/*`; workload identity, network reachability, rate/concurrency budget and readiness are isolated. Optional ObjectStore/provider/Redis failure disables only dependent capabilities and does not remove every otherwise-safe API replica | `V1_TECHNICAL_SOLUTION_PROPOSAL.md`; Proposed ADR-0022 | Proposed | Unverified | Platform/application owner TBD |
| FV1-ROADMAP-REVIEW | ObjectStore current, mandatory H0 business/DataSafety and separately applicable Availability gates | Health / Isolation / Recovery | Unknown | H0 object slice; Availability only when `UD-AVL-01` applies | Current MinIO service health does not pass business auth: latest live check is `InvalidAccessKeyId`. H0 requires least-privilege application identity, bucket/purpose isolation, TLS/encryption/rotation, quarantine/immutable version/hash/grant/deny/delete, lifecycle/capacity, backup/version, consistent-cut recovery/non-resurrection and restore evidence. Multi-fault-domain durability and N-1 belong only to the separately approved AvailabilityGate. Until each due gate passes, only object-dependent capabilities are disabled | `V1_SERVICE_MIDDLEWARE_AND_OPERATIONS_DESIGN.md`; Proposed ADR-0015/0018/0022 | Proposed | Unverified | Object/data/platform owners TBD |
| FV1-ROADMAP-REVIEW | Worker claim, JIT call, retire and DeliveryStore loop | Retry / Idempotency / Capacity / Recovery | Unknown | Before first asynchronous business workload | API owns pool isolation and bounded aging/fairness; Worker honors `nextClaimNotBefore` with one bounded backoff+jitter owner. Heartbeat and POST grant commands have stable ID/sequence/digest outcome-recovery; GET inputs is side-effect free. Artifact payload+immutable report envelope+record+unreceipted-index entry share one durability boundary before RESULT_BUFFERED，record以job/context/reportKey唯一且同key异hash拒绝。每个buffer写grant先写冻结预分配record/payload locator与envelope/result hash的耐久grant-intent receipt。Producer在首次report前丢失时只能签DELIVERY_RECOVERY重报原record；report响应丢失按job+reportKey找回receipt。Index以稳定snapshot/cursor、sequence/HWM完整扫描，gap/lag/不可证使pool/reconciliation/delete cleanup fail closed。删除barrier停止普通WORK/grant；仅pre-barrier CALL_START_COMMITTED可取得无provider/原输入权限的DELETION_DISPOSITION lease及隔离buffer。无payload分支必须证明全部相关lease/grant失效、逐个已签locator从未可见或已secure erase、固定HWM无record且无竞态，才写`NO_PAYLOAD_DISPOSITION_ACCEPTED/OUTCOME_UNKNOWN_NO_PAYLOAD`；该proof不表示provider未处理。Full/corrupt/expired store or attempt/age/unknown-outcome limit stops the affected pool and enters WAITING_DIAGNOSIS/RETIRED; no unreceipted result or possible paid call is dropped/replayed | `V1_SERVICE_MIDDLEWARE_AND_OPERATIONS_DESIGN.md`; Proposed ADR-0013/0018/0029 | Proposed | Unverified | Execution/operations owner TBD |
| FV1-ROADMAP-REVIEW | Common degradation, freshness and retry contract | Degradation / Retry / Isolation | Unknown | H0 and cumulative H1/H2 | Every query/receipt/error carries degradation mode, affected capabilities, data freshness/asOf, optional authorized last-known-good ref and retryable/retryAfter. Unknown/stale/LKG authority disables formal/release/decision/AI/object-finalize while authorized history may remain explicitly read-only. GET auto-retry requires server `retryable=true` plus an approved endpoint policy and bounded budget, and honors Retry-After; until then manual recovery is the default, including transport failures without an authoritative response. Formal commands/paid AI/finalize/unknown outcomes query the same receipt/intent and never blindly create a new key | `V1_DATA_AND_INTERFACE_CONTRACT_DESIGN.md`; `../uiux/SYSTEM_DEGRADATION_AND_RECOVERY_UIUX.md` | Proposed | Unverified | API/Web/reliability owners TBD |
| FV1-ROADMAP-REVIEW | Redis/Timescale triggered activation | Isolation / Recovery / Capacity | Unknown | Any first business use | Redis remains zero-authoritative-consumer and TimescaleDB creates no V1 extension/schema until measured trigger, owner, ADR, physical allowlist, migration/backfill, capacity/recovery and rollback are approved. Each Redis role is activated separately with explicit failure semantics; trigger evidence is not implementation approval | `V1_SERVICE_MIDDLEWARE_AND_OPERATIONS_DESIGN.md`; Proposed ADR-0023 | Proposed | Unverified | Data/platform/application owners TBD |
| FV1-PRODUCT-DESIGN | Draft save and interrupted session | Recovery | Required | Implementation | Preserve input on save failure/offline/stale state; restore latest saved draft and partial result after browser/network/service interruption | PRD v1.1 sections 7.5 and 7.9 | Confirmed | NotYetTested | Draft owner TBD |
| FV1-PRODUCT-DESIGN | Task deletion and retained evidence | Data lifecycle | Required | Pre-release | Immediate user inaccessibility; task user data 7 days; backups 30 days; non-content security/admin audit metadata 180 days | PRD v1.1 section 7.9 | Confirmed | NotYetTested | Security/data owner TBD |
| FV1-BOOTSTRAP | API liveness and PostgreSQL readiness | Health / deadline / degradation | Required | Bootstrap | Liveness performs no downstream probe; readiness uses a finite 2-second PostgreSQL probe and returns 503 when unavailable; no probes PostgreSQL; readiness returns 503 and dependency detail reports degraded | User bootstrap approval; ADR-0002 | Confirmed | Passed for configuration/degraded paths and a real PostgreSQL-ready local run on 2026-07-30 | API service |
| FV1-DIAGNOSTIC-BOOTSTRAP | Worker PostgreSQL status probe | Deadline / Health | Required | Bootstrap | One bounded probe with 2 s default and 10 s configured maximum; no automatic retry; internal status returns 503 with a classified reason when unavailable; probe always closes | User diagnostic approval; ADR-0005 | Confirmed | Passed in unit tests and a real PostgreSQL-ready local run on 2026-07-30 | Worker service |
| FV1-DIAGNOSTIC-BOOTSTRAP | Web-to-API-to-Worker diagnostic chain | Deadline / Retry / Degradation | Required | Bootstrap | API calls Worker with a two-second timeout and zero retries; request ID propagates; full chain returns 503 with truthful per-service reasons when either PostgreSQL probe or Worker is unavailable; Web makes one cancellable request and never polls | User diagnostic approval; ADR-0005 | Confirmed | Passed for unit, native degraded and real PostgreSQL-ready paths; the Web-proxied chain returned 200 on 2026-07-30 | API service |
| FV1-BOOTSTRAP | API/Worker source and module isolation | Isolation | Required | Bootstrap | Separate manifests, locks, namespaces and images; no direct cross-service source import; nine modules have singular code-service owners; production module graph remains acyclic | User instruction 2026-07-15; ADR-0002 | Confirmed | Passed | Architecture owner |
| FV1-LOCAL-TEST-DEPLOY | Local deployment entry delegation | Isolation / Recovery | Required | Bootstrap | One wrapper delegates to the registered native launcher without duplicating service commands; no Docker/cloud dependency; non-zero child exit propagates; launcher cleanup owns only its child processes | User local-test deployment instruction; ADR-0006 | Confirmed | Wrapper preflight and PostgreSQL-ready native chain Passed | Platform/developer-experience owner |
| FV1-REMOTE-MIDDLEWARE-DEV | Local-to-server middleware SSH tunnel | Isolation / Deadline / Health / Recovery | Required | Bootstrap | Local forwards bind only to `127.0.0.1`; SSH host-key verification remains enabled; connection setup has a 10-second timeout and `ExitOnForwardFailure`; dead sessions are detected through bounded keepalive; no automatic retry or credential persistence; closing the foreground SSH process removes all four forwards; server middleware ports remain loopback-only | User local-development instruction 2026-07-24; existing ADR-0007 private-port boundary | Confirmed | PowerShell parse, no-network `-ValidateOnly`, duplicate-port rejection and live forwarding Passed | Platform/developer-experience owner |
| FV1-LOCAL-MIDDLEWARE-DIAGNOSTIC | Authenticated local middleware check | Deadline / Health / Isolation / Security | Required | Bootstrap | PostgreSQL, Redis and MinIO are checked concurrently with three-second default/ten-second maximum timeouts and zero retries; probes use `SELECT 1`, `AUTH`/`PING` and signed read-only `ListBuckets`; any failure exits non-zero; output omits credentials, response bodies and bucket names | User local diagnostic instruction 2026-07-29; ADR-0010 | Confirmed | Unit, lint, type and configuration-failure paths Passed; PostgreSQL and Redis live authentication Passed on 2026-07-30; MinIO returned `InvalidAccessKeyId`, so the complete three-service result remains Unverified | API service / platform owner |
| FV1-LOCAL-RUNTIME / FV1-DIAGNOSTIC-BOOTSTRAP | Native local process startup | Configuration / Health | Required | Bootstrap | Local startup uses no Docker; optional root `.env` never overwrites process values or prints values; `all` starts hidden API/Worker child processes, waits for liveness, keeps Web foreground and cleans up exact children on exit; Windows uses a selector event loop for async psycopg compatibility | User instructions; ADR-0004; ADR-0005; ADR-0006 | Confirmed | Preflight, degraded smoke and a PostgreSQL-ready native three-service run Passed; IPv4 Web/API/Worker listeners and the full 200 chain were observed on 2026-07-30 | Platform/developer-experience owner |
| FV1-SERVER-MIDDLEWARE-DEPLOY / FV1-SERVER-DATA-EXTENSIONS | PostgreSQL with pgvector/TimescaleDB, Redis and MinIO single-host runtime | Isolation / Health / Capacity / Recovery | Required | Bootstrap | Secret values are untracked root-only file mounts; Redis reads its secret in a short root wrapper before the official entrypoint drops the server process to `redis`; one-command bootstrap fails fast, never prints or replaces a non-empty secret, validates/builds and waits for health; build context excludes all repository files; ports bind to loopback by default; all services have health checks, bounded logs, restart/process/CPU/memory limits and named volumes; the lightweight profile keeps internal memory below container limits and preloads at most two TimescaleDB workers within six PostgreSQL workers, but no extension/schema is auto-created; routine `down` preserves volumes and destructive `down -v` is forbidden; host disk quota/alerts, extension-aware backup/restore, TLS, monitoring and HA remain required follow-up controls | User server-middleware, extension and capacity instructions; ADR-0007/0008/0009 | Confirmed | Static repository configuration/script syntax and one target-server image-build/three-service-health smoke Confirmed; extension SQL query, sustained health, host disk enforcement and recovery exercise Unverified | Platform/operations owner |

Control types include Deadline, Cancellation, Retry, Idempotency, Degradation, Isolation, Capacity/Backpressure, Health, Observability, and Recovery.

## Result Registry

| Scope ID | Target/control row | Build and environment | Scenario / command | Raw result and sample/window | Evidence | Verification status | Date |
|---|---|---|---|---|---|---|---|
| FV1-BOOTSTRAP | API PostgreSQL readiness probe | Local Windows; Python 3.13.14; no PostgreSQL configured | API unit command and real Uvicorn startup | 7 non-integration tests passed; `/health/live` 200; `/health/ready` 503; `/health/dependencies` 200 degraded; one execution | `services/api/tests/unit/`; local command output | Passed for configured-absent degradation; successful database path Unverified | 2026-07-15 |
| FV1-BOOTSTRAP | Worker PostgreSQL startup probe | Local Windows; Python 3.13.14; no PostgreSQL configured | Worker unit command and `python -m flowverse_worker --check` | 3 tests passed; real process emitted structured configuration failure and non-success exit; one execution | `services/worker/tests/unit/`; local command output | Passed for configuration-failure behavior; successful database path Unverified | 2026-07-15 |
| FV1-BOOTSTRAP | API/Worker source and module isolation | Repository source on Windows | `python scripts/check_architecture.py` | 2 Python code services; 9 singular module owners; 0 cross-module dependencies | `scripts/check_architecture.py`; checked-in service trees | Passed | 2026-07-15 |
| FV1-LOCAL-RUNTIME | Native local process startup | Local Windows; Python 3.13.14; root `.env` and PostgreSQL absent; Web source/lock absent | `scripts/start-local.ps1 preflight`; foreground `api` plus three health requests; `worker-check`; guarded `web` | Preflight exited 0 and reported Docker unused; API started on `127.0.0.1:8000`, liveness 200, readiness 503 and dependency detail 200 degraded; Worker emitted structured configuration failure; Web stopped on missing lockfile; one execution each | `scripts/start-local.ps1`; local command output | Passed for available and explicit-failure paths; configured PostgreSQL, runnable Web and long-running Worker remain Unverified | 2026-07-16 |
| FV1-DIAGNOSTIC-BOOTSTRAP | Native Web → API → Worker chain | Local Windows; Python 3.13.14; Node.js 24.17.0; PostgreSQL and root `.env` absent | Start three real service processes; request Web `/`, API chain and Web-proxied chain; stop exact processes | Web 200; API chain 503; Web proxy chain 503; both chain responses matched and reported API/Worker `configuration`; one execution | Service processes and HTTP response captured in local command output | Passed for reachability, proxy and truthful degradation; PostgreSQL-ready path Unverified | 2026-07-21 |
| FV1-LOCAL-TEST-DEPLOY | Local deployment wrapper | Local Windows; Python 3.13.14; Node.js 24.17.0; root `.env` absent | `powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -File deploy/local/start.ps1 preflight` | Exit 0; API/Worker runtimes, Web lock/source and exact Node runtime reported ready; Docker reported unused; one execution | `deploy/local/start.ps1`; local command output | Passed | 2026-07-21 |
| FV1-SERVER-MIDDLEWARE-DEPLOY / FV1-SERVER-DATA-EXTENSIONS | Single-host middleware image build and health | Target Aliyun Linux server; Docker Compose; one observed deployment | Compose validation/build/start sequence followed by `docker ps`; initial PostgreSQL version-suffix, MinIO network/metadata and Redis root-only-secret failures were corrected in the deployment source | Corrected PostgreSQL and MinIO images built; `docker ps` reported PostgreSQL, Redis and MinIO `healthy`; one short smoke observation | User-supplied target-server command output/screenshots; `deploy/server/middleware/` | Passed for build/start/current health; extension SQL query, sustained operation, workload capacity and recovery Unverified | 2026-07-23 |
| FV1-REMOTE-MIDDLEWARE-DEV | Local middleware SSH tunnel validation | Local Windows PowerShell; system OpenSSH present; no remote connection attempted | Registered `-ValidateOnly` command plus a duplicate-local-port invocation | Default four local-only mappings validated with exit 0; duplicate ports were rejected with non-zero exit and the expected classification; one execution each | `deploy/local/start-middleware-tunnel.ps1`; local command output | Passed for local configuration/failure path; live SSH forwarding Unverified | 2026-07-24 |
| FV1-LOCAL-MIDDLEWARE-DIAGNOSTIC | Local middleware authentication implementation | Local Windows; Python 3.13.14; four tunnel ports currently reachable; root `.env` absent | API lint/format/type/unit commands, four PowerShell parser checks, registered `middleware-check`, and native API/Worker smoke | Ruff and Pyright passed; 22 API unit tests passed including non-loopback rejection; scripts parsed; missing credentials truthfully returned three `configuration` results and exit 1; API/Worker liveness returned 200 and the unconfigured chain truthfully returned 503; no secret output | Source/tests and local command output | Passed for implementation, security boundary and configuration failure; live authenticated run Unverified | 2026-07-29 |
| FV1-LOCAL-RUNTIME / FV1-LOCAL-MIDDLEWARE-DIAGNOSTIC | Windows native PostgreSQL-ready chain | Local Windows; Python 3.13.14; SSH forwards open; ignored `.env` configured | Runner unit tests, middleware check, native full-stack start, direct and Web-proxied HTTP checks | PostgreSQL and Redis reported ready; MinIO truthfully failed with `InvalidAccessKeyId`; Web, API and Worker listened on configured IPv4 addresses; both direct and Web-proxied chain endpoints returned 200 | Source/tests and local command output | Passed for PostgreSQL-ready application chain; complete three-middleware authentication remains Unverified until the MinIO user is corrected | 2026-07-30 |

Targets and budgets never prove results. Passed requires raw monitoring, test, restore, or failure-exercise evidence tied to the relevant row.

## Fixed Reliability Rules

### Deadlines and Retry

- Every remote call, background task, lock wait, and stream has a finite lifecycle.
- Retry only classified transient failures and respect cancellation, rate-limit signals, concurrency, resource budgets, finite attempts, and a finite total deadline.
- Never use a tight retry loop; Confirmed policy defines attempt limit, delay/backoff, jitter, and total elapsed limit.
- Exactly one layer owns automatic retry for a call chain; avoid SDK/client/gateway/task retry multiplication.
- Do not retry permission, validation, business rejection, deterministic failure, or non-idempotent side effects automatically.
- Side-effect retry requires Confirmed idempotency/deduplication covering key scope, concurrent duplicates, stored/replayed result, authoritative store, and retention/expiry window.

### Degradation and Isolation

- Degradation is explicit, observable, reversible, and approved for the affected capability.
- Never fake success, silently lose data, bypass authorization, or weaken integrity as degradation.
- Bound queues, pools, concurrency, backlog, cache, fan-out, and tenant/task resource use; apply Confirmed backpressure where required.
- Circuit breakers, bulkheads, read-only mode, queues, and caches are optional patterns, never AI defaults.
- Isolate a failing dependency/workload only when confirmed failure-domain and capacity evidence justify it.

### Health and Observability

- Liveness indicates whether the process itself is irrecoverably stuck; downstream failure alone must not cause restart storms.
- Readiness indicates whether the instance can safely accept traffic under its Confirmed dependency and initialization policy.
- Startup indicates whether required one-time initialization is complete before liveness/readiness enforcement.
- Health checks are lightweight, bounded, non-destructive, and do not expose secrets.
- Critical flows have correlated structured logs, metrics, and traces where applicable.
- Logs exclude secrets and unauthorized personal data; metric labels have bounded cardinality.
- Tool, sampling, retention, alerts, and ownership remain Unknown until approved.

### Data Protection and Recovery

- A backup does not prove recoverability.
- Passed recovery requires a restore drill, integrity checks, elapsed time, and measured data loss against the data-set RTO/RPO row.
- Migrations are repeatable or recoverable, bounded, observable, and compatible with the Confirmed deployment sequence.
- Destructive cleanup occurs only after compatibility and recovery gates pass.
- Backup frequency, retention, encryption, location, restore cadence, and ownership remain Unknown until approved.

### Failure Testing

- Test applicable timeout, dependency outage, rate limit, duplicate, reordering, partial success, restart, saturation, and recovery paths.
- Do not inject faults into production without explicit approval and a bounded safety plan.
- A failed required failure/recovery test is Failed; unavailable evidence is Unverified, never Passed.

## Stage Gates

- Intake: identify critical flows, recoverable data classes, dependencies, expected load, Applicability, owners, and recovery priorities.
- Implementation: Confirm affected deadline, retry owner, idempotency, degradation, isolation, capacity, health, and observability target rows.
- Pre-release: every Required due row has Confirmed target and Passed verification, including capacity, health, alerts, recovery, compatibility, and failure tests.
- Evolution: every API/schema/event/config change states mixed-version impact, release order, migration, removal conditions, and recovery evidence.

## Relationship to Performance

- Reliability owns operational safety limits and failure semantics.
- Performance measures latency, throughput, interaction, bundle, and memory while respecting those limits.
- A performance optimization cannot weaken reliability, and a reliability pattern cannot bypass a Confirmed performance budget.
